# 組み分けぼうし

## アプリを作った理由

 　懇親会などで、アプリを使用してグループ分けをした際に同じメンバーと被ることがあり、
 話したことのない人とは別グループになってしまった経緯から、一度同じグループになった人とは
 極力被らないアプリを作成しようと思いました。
 　また、以前作成したミニアプリは見た目を極力シンプルにしたので今回は、見た目に力をいれようと思いました。

## できること（機能一覧）

 　メンバーを入力し、組数をしていすると、組み分けを行います。
 　2回目以降は、同じメンバーと被らないように、被り防止機能をONにできます。

## 技術スタック

- Ruby on Rails

## 今後の改善ポイント

・組み分けの結果をdbに保存し被り防止機能を実装

## デプロイ

今後のアップデート案
・ルーレット機能のついか
・ユーザーログイン機能のついか
・CSVの読み込み、出力機能のついか

## 以下 記録

アプリの画面遷移図は手書きのため、省略
[Splash]
    ↓
[HOME]
    ↓
[名簿作成]
    ↓
[組数入力]
    ↓
[組み分け設定]
    ↓
[組み分け演出]
    ↓
[結果]
    ↓
[HOME]

今後HOMEから分岐し機能を追加していく

使用技術は基本的にRailsで、cssとjsをメインにする
今回はReactは使わないこととする。
SNSのような頻回な画面切り替えはないアプリのため、Reactをいれると
コードが複雑かつ重くなるため。

背景画像とキャラクターとタイトルロゴはAIで画像生成したものを使用。

触っていて、楽しい、便利なアプリを目指して作成します。

ｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰｰ
※会話のループが狙った通りにいかない
会話をクリックで進めることから、はい いいえの選択肢のちょうどで
想定した文章を表示させることができなかったが、特定のセリフをきっかけに出現させることで
ループから抜けることとした。

組み分け画面から名簿入力へ移動するための「入力する」ボタンを、「 link_to」で作成していたら、
javaScriptによって、HTMLに変換されてしまっていたためにボタンが機能しなかった。
なので、JavaScriptに直接リンク先のURLを書き込むことにした。

セリフと選択肢が増えてきたので、ルールを見直し
普通のセリフはクリックで次へ進む。選択肢が必要なセリフはセリフ表示から1秒後に選択肢。
選択肢はクリックで分岐選択。次のセリフはクリックで進む。
このルールに統一する。

組み分け履歴の保存について
今回は、DBは作らずRailsのsessionに保存することにする。DBをつくると永久にサーバーに保存されてしまうため負荷がかかるのと、組み分け履歴は際限なく増え続けるため。sessionとして保存する履歴は、理論上の被り防止回数上限が終了したら、または任意リセットのときに履歴を削除することにした。

組み分けは、なるべく違う人と会うように設定しているが、回数を重ねると可能な組み合わせが減っていくため、理論上の限界回数がある。
しかし、単純な割り算では求めることができない。端数やその調整で同じ人と組み分けされることがあるため。そのため理論上の上限がきたら判定しアクションを起こす必要がある。

上限について
基本ルール：履歴から完全に新しい組み合わせをさがす。しかしこれには穴があり、人数をグループ数で割り切れないときにこのルールだとペアになっていない状態で上限に達して終了してしまう。
そこで、上限ルールを二重にする。第一条件は上記として、その時点の上限にきたら、「全員と一度ペアを組んだのかを履歴から確認し消化する」を第二の条件に最終上限とした。

----------------------

1000回試す
 ↓
一番良かったものを保存（best_score）
 ↓
最後に返す　被りはnil

これだと上記のような穴ができる

best_groups = nil（被りなしでは作れないとき）
best_score = -1　（最善）

としていたが、この理由により削除とした

----------------------

このルールなら終盤は被りは多少出るが、全員と組になることができる。
アプリ内では説明は省略するため、終盤には「被りがでている」と思うかもしれない。が、「なるべく被らないようにする魔法」と説明しているので良しとした。

ルートの分岐とモード選択によって複雑化してしまいエラーが増えたので、内容整理をした。ノーマルモードをfalse組み分け防止モードをtureとしbooleranでON・OFFを管理することにした。これにより簡略化できルートもわかりやすくなった。
